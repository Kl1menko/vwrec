{
  "name": "vwrecruit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:pages": "node scripts/generate-pages.mjs",
    "dev": "npm run sync:pages && vite",
    "build": "npm run sync:pages && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.93.2",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "circle-flags": "^2.8.2",
    "cobe": "^2.0.1"
  }
}
