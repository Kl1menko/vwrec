import { createLocaleContent } from '../create-locale-content.js'

export const ltContent = createLocaleContent({
  code: 'lt',
  name: 'Lithuanian',
  nativeName: 'Lietuvių',
  heroTitle: 'International recruitment website starter for Lithuanian-language expansion.',
  heroLead:
    'Locale structure, page generation, and reusable sections are prepared so the full translation layer can be filled in next.',
})
